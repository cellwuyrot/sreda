<?xml version="1.0" encoding="utf-8"?>
<!--
  FIX-BOOT: всё, что касается службы доставки уведомлений.

  Этот манифест подмешивается только когда рядом лежит файл доступов
  app/google-services.json. Без файла в сборке нет ни службы, ни самой
  библиотеки доставки — и приложению нечему падать на старте.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <service
            android:name=".PushService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="tz_connect_messages" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
    </application>
</manifest>
