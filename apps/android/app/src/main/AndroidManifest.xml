<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core connectivity: the shell renders a remote site. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Voice channels & video: microphone and camera (WebRTC). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Location sharing on the map. Optional — the app degrades gracefully. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Android 13+ runtime notification permission (in-app toasts and push). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      CALL: входящий звонок поверх блокировки.
    -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
      VPN-ANDROID: права для нативного VPN-сервиса.

      BIND_VPN_SERVICE — обязательно для любого VpnService: без него система
      отказывает сервису в доступе к сетевому стеку. Нельзя объявить вручную
      от имени приложения — он выдаётся системой через VpnService.prepare().

      FOREGROUND_SERVICE + FOREGROUND_SERVICE_SPECIAL_USE — VPN-сервис обязан
      быть foreground, иначе Android его убьёт через несколько минут.
      На Android 14+ нужен тип специального использования (FOREGROUND_SERVICE_TYPE_SPECIAL_USE)
      для сервисов, не попадающих в стандартные категории.

      CHANGE_NETWORK_STATE / CHANGE_WIFI_STATE — некоторые версии AmneziaWG-Go
      используют их для управления маршрутами.
    -->
    <uses-permission android:name="android.permission.BIND_VPN_SERVICE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Nothing here is mandatory, so the app installs on all devices. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />

    <application
        android:name=".ConnectApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.Connect"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|keyboard|uiMode|navigation"
            android:hardwareAccelerated="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Connect.Splash"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          VPN-ANDROID: нативный VPN-сервис на базе AmneziaWG.

          permission="android.permission.BIND_VPN_SERVICE" — защищает сервис от
          запуска произвольными приложениями: только система и наше приложение
          могут его запустить.

          foregroundServiceType="specialUse" — Android 14+ требует явного типа
          для foreground-сервисов. «specialUse» — это тип для VPN и подобных
          сервисов, не попадающих в стандартные категории (camera/microphone/etc).
        -->
        <service
            android:name=".AmneziaVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <!-- Android 14+ requires a <property> declaring the use case. -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="VPN tunnel via AmneziaWG" />
        </service>

        <activity
            android:name=".IncomingCallActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.Connect" />

    </application>
</manifest>
