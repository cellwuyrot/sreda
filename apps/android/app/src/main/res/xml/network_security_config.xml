<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- FIX-SEC: послабление для локальных адресов перенесено в
         <debug-overrides>. Раньше оно действовало и в выпускной сборке:
         приложение на телефоне было готово ходить по HTTP на localhost, а туда
         может слушать любое другое приложение на том же устройстве.
         debug-overrides действует только при android:debuggable="true". -->
    <debug-overrides>
        <domain-config cleartextTrafficPermitted="true">
            <domain includeSubdomains="false">10.0.2.2</domain>
            <domain includeSubdomains="false">localhost</domain>
            <domain includeSubdomains="false">127.0.0.1</domain>
        </domain-config>
    </debug-overrides>
</network-security-config>
