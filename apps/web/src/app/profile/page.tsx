import ProfilePage from "@/components/profile/ProfilePage";

/** PROFILE-WALL: своя страница. Имя берётся из сессии на клиенте. */
export default function MyProfileRoute() {
  return <ProfilePage />;
}
