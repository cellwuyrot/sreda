/**
 * NETLINK: единые названия услуги для всего проекта.
 *
 * Слова «VPN» в интерфейсе больше нет. Причина не косметическая: название
 * услуги встречалось в десятках мест разными словами, и при любой смене
 * формулировки часть надписей отставала молча. Здесь одно место правды.
 *
 * ВАЖНО: переименована только терминология для человека. Имена таблиц, полей,
 * маршрутов и функций (`VpnPeer`, `/api/vpn/me`, `hasVpnEntitlement`) остались прежними
 * намеренно: переименование схемы и путей — это миграция базы и обрыв старых
 * клиентов (Android-оболочка и десктоп обновляются не в один день с сайтом) ради
 * слова, которое пользователь всё равно не видит.
 *
 * Файл без `prisma` и без реактивных зависимостей: его импортируют и серверные
 * маршруты, и клиентские компоненты.
 */

/** Как называется сама возможность — защищённый канал до серверов TZ. */
export const LINK_NAME = "Надёжное соединение";
/** То же с маленькой буквы — для середины фразы. */
export const LINK_NAME_LOWER = "надёжное соединение";
/** Родительный падеж: «статус надёжного соединения». */
export const LINK_NAME_GENITIVE = "надёжного соединения";
/** Название отдельной подписки (раньше — «только VPN»). */
export const LINK_PLAN_NAME = "Ускоренный интернет";
/** Как упоминать подписку в кавычках внутри фразы. */
export const LINK_PLAN_QUOTED = `«${LINK_PLAN_NAME}»`;
