-- FIX-SEC: отметка смены пароля.
--
-- Без неё смена пароля не завершала уже выданные сессии: токен в cookie
-- подписан и проверяется без обращения к базе. NULL у существующих записей
-- означает «пароль не меняли» — текущие сессии остаются действительными.
ALTER TABLE "User" ADD COLUMN IF NOT EXISTS "passwordChangedAt" TIMESTAMP(3);
